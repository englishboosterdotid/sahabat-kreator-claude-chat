export type ContentIdea = {
  hook: string;      // kalimat pembuka yang menarik perhatian
  pillar: string;     // kategori/tema besar konten (edukasi, promosi, storytelling, dst)
  angle: string;      // sudut pandang spesifik ide ini
};